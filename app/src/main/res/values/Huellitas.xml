<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema general de la app -->
    <style name="Theme.Huellitas" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Personaliza colores aquí -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@android:color/white</item>
    </style>

    <!-- Tema sin barra para Splash -->
    <style name="Theme.Huellitas.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/purple_200</item>
        <item name="android:statusBarColor">@color/purple_700</item>
    </style>

</resources>
